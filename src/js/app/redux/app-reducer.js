/**
 *
 * Редьюсер приложения
 *
*/

const
    actionTypes = new Map([

    ]);

const initialState = {

};


// Reducer

const appReducer = (state=initialState, action) => {

    switch (action.type) {
        default:
            return state;
    }

};

export default appReducer;


// Actions


// Thunks